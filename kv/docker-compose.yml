services:
  fastapi: 
    build: .
    image: riviga77/eoi-fastapi-kv
    container_name: eoi-fastapi-kv
    ports:
      - "8000:8000"
    volumes:
      - .:/code
  
  redis:
    image: "redis:latest"
    container_name: redis    
    volumes:
      - ./data:/data